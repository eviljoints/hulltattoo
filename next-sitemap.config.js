module.exports = {
  siteUrl: 'https://hulltattoostudio.com', // Replace with your website's URL
  generateRobotsTxt: true, // Automatically generate a robots.txt file
  generateIndexSitemap: true, // Generates a sitemap index if needed
  exclude: ['/admin', '/hidden-page'], // Add pages to exclude if necessary
  changefreq: 'daily', // Default change frequency
  priority: 0.7, // Default priority
}; 